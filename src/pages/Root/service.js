export const getBowlingTeamId = gameState => {
  // some processing

  return gameState || null;
};

export const getBattingTeamId = gameState => {
  // some processing

  return gameState || null;
};

export const getHomeTeamName = streamData => {
  // some processing

  return streamData || null;
};

export const getAwayTeamName = streamData => {
  // some processing

  return streamData || null;
};
